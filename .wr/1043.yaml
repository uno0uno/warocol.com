# Machine contract — issue #1043
issue: 1043
title: "feat(menu): single-screen product create page (remove wizard)"
repo: front_nuxt
repo_remote: uno0uno/warocol.com
cwd: /Users/saifer/Documents/WEBS/WARO COLOMBIA/front_nuxt
stack: nuxt
branch: wr-1043-single-page-create

paths:
  - pages/menu/productos/crear.vue
  - components/ui/FormSection.vue
  - components/menu/ProductResaleCreateForm.vue

ac:
  - No wizard steps, Atrás/Siguiente, or REVISION step
  - Type cards (Con receta / Venta directa) always visible at top; toggle sections
  - Layout form.grid xl:grid-cols-3 — left UiFormSection card, right sticky Resumen + Crear producto
  - Microcopy aligned with productos/[id].vue (Nombre *, Foto, Precio *, etc.)
  - submitProduct unchanged (resale sync, ingredients, recipe bases, modals)
  - validateForm at submit — name, category, price, resale weight, ingredients; duplicate name check
  - Deep link ?modo=venta-directa preserved
  - Badge Reventa on Datos when isResaleDirectMode
  - Recipe sections only when !isResaleDirectMode inside MenuCatalogInlineCreateBusyOverlay
  - Resale MenuProductResaleCreateForm in Inventario UiFormSection (embedded)
  - Sidebar — price, cost, margin, type badge, recipe lines or equivalencia, submit + cancel
  - Fade transition styles removed

out_of_scope:
  - ProductQuickCreatePanel
  - API changes
  - pages/menu/productos/[id].vue

hints:
  entry: "pages/menu/productos/crear.vue — mirror [id].vue grid layout"
  pattern: "@submit.prevent=\"submitProduct\" + validateForm()"
  deep_link: "?modo=venta-directa"
  tests: none

skip:
  audits: [ux, color, typography]
