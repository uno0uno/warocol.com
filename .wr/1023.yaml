issue: 1023
title: "feat(pos): BOGO bundle lines with per-unit modifiers (online cart parity)"
repo: both
repo_remote: uno0uno/warocol.com
cwd: /Users/saifer/Documents/WEBS/WARO COLOMBIA/front_nuxt
stack: both
branch: wr-1023-bogo-per-unit-lines

paths:
  - pages/pos/producto/[id].vue
  - stores/usePOSStore.ts
  - pages/pos/index.vue
  - ../api_warocol.com/app/services/promotions_service.py
  - ../api_warocol.com/tests/test_promo_cart_evaluation.py

ac:
  - Waiter configures distinct modifiers per BOGO unit (wizard or duplicate-line UX)
  - API groups sibling lines by product_id, forms buy_qty+get_qty sets, marks free units cheapest-first
  - Comanda + checkout receive per-unit modifier detail on separate qty=1 lines
  - Parity note vs online_cart separate-line model documented in code or PR

out_of_scope:
  - Online storefront UI changes
  - Fiscal invoice / DIAN (#1025)
  - Cart footer net total (#1022 — shipped)
  - Comanda KDS split logic (#1021 — shipped on aggregated lines)

hints:
  entry: "producto/[id].vue addToCart — wizard uses addCartItemsBatch"
  online_pattern: "online_cart.addItemsBatch — one row per modifier config"
  api: "promotions_service._allocate_bogo_savings_cheapest_first — cross-line BOGO"
  tests: "pytest tests/test_promo_cart_evaluation.py -v"

skip:
  audits: [ux, color, typography]
