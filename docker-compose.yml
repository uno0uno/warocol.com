services:
  warocol:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        NODE_BUILD_MAX_OLD_SPACE_SIZE: ${NODE_BUILD_MAX_OLD_SPACE_SIZE:-6144}
      secrets:
        - env_file
    volumes:
      - nuxt-data:/app/storage
    networks:
      - warolabs-network

secrets:
  env_file:
    file: .env

networks:
  warolabs-network:
    external: true

volumes:
  nuxt-data:
